{
  "name": "gesture-matter",
  "version": "1.0.0",
  "private": true,
  "description": "Two-hand material and physics playground",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check physics.js && node --check render.js && node --check intent-model.js && node --check intent-worker.js && node --check intent-assist.js && node --check server.js",
    "test": "node --test tests/physics.test.cjs tests/intent.test.cjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35",
    "matter-js": "0.20.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
