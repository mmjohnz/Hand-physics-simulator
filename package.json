{
  "name": "gesture-matter",
  "version": "1.0.0",
  "private": true,
  "description": "Two-hand material and physics playground",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check physics.js && node --check render.js && node --check server.js",
    "test": "node --test tests/physics.test.cjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "matter-js": "0.20.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
